---
wave: 1
depends_on: []
files_modified: ["server/package.json", "server/prisma/schema.prisma", "server/src/index.ts"]
autonomous: true
---

# Plan 01-01: Backend setup (Express, Prisma schema, PostgreSQL init)

## Goal
Set up the Express.js server, Prisma ORM, and the database schema for Users.

## Tasks

<task>
  <action>Initialize the `server` directory with Express.js and TypeScript, and set up Prisma.</action>
  <read_first>None</read_first>
  <acceptance_criteria>
    - `server/package.json` exists with `express` and `prisma` dependencies.
    - `server/prisma/schema.prisma` exists and contains the `User` and `CollectorProfile` models with Customer and Collector roles.
  </acceptance_criteria>
</task>

<task>
  <action>Create a basic Express server entry point in `server/src/index.ts` with CORS configured.</action>
  <read_first>server/package.json</read_first>
  <acceptance_criteria>
    - `server/src/index.ts` exists and listens on port 3001.
    - CORS is configured with `credentials: true` and `origin: 'http://localhost:3000'`.
  </acceptance_criteria>
</task>

<task>
  <action>[BLOCKING] Run `npx prisma db push` to push the schema to the database.</action>
  <read_first>server/prisma/schema.prisma</read_first>
  <acceptance_criteria>
    - Database tables are created.
  </acceptance_criteria>
</task>

## Verification
- Run `npm run dev` in the `server` directory and verify the server starts on port 3001.
