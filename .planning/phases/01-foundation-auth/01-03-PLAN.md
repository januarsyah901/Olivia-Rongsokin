---
wave: 3
depends_on: ["01-02-PLAN.md"]
files_modified: ["client/package.json", "client/app/login/page.tsx", "client/app/register/page.tsx"]
autonomous: true
---

# Plan 01-03: Frontend Auth UI and Profile Setup Forms

## Goal
Build the Next.js frontend pages for login and registration.

## Tasks

<task>
  <action>Initialize Next.js app in the `client` directory (using App Router and Tailwind).</action>
  <read_first>None</read_first>
  <acceptance_criteria>
    - `client/package.json` exists.
    - `client/app/layout.tsx` exists.
  </acceptance_criteria>
</task>

<task>
  <action>Create Registration Page (`client/app/register/page.tsx`) with role selection (Customer/Collector) that submits to the backend API (`http://localhost:3001/api/auth/register`).</action>
  <read_first>client/package.json</read_first>
  <acceptance_criteria>
    - `client/app/register/page.tsx` exists and contains a form with a role selector (CUSTOMER or COLLECTOR).
    - Uses `fetch` or `axios` to submit credentials.
  </acceptance_criteria>
</task>

<task>
  <action>Create Login Page (`client/app/login/page.tsx`) that submits to the backend API and redirects to home.</action>
  <read_first>client/package.json</read_first>
  <acceptance_criteria>
    - `client/app/login/page.tsx` exists and handles form submission.
    - Successful login redirects to `/`.
  </acceptance_criteria>
</task>

## Verification
- Run `npm run dev` in `client`, go to `/register` and `/login`, and verify the forms render correctly.
